# 🧪 دليل اختبار المرحلة الثالثة

## ✅ البناء نجح!

تم بناء التطبيق بنجاح. الملف الناتج:
```
build\app\outputs\flutter-apk\app-debug.apk
```

---

## 📱 خطوات التثبيت والاختبار

### 1️⃣ تثبيت التطبيق

**على جهاز حقيقي:**
```bash
flutter install
```

**أو يدوياً:**
- انسخ الملف `app-debug.apk` إلى هاتفك
- افتح الملف وثبّت التطبيق

**على محاكي:**
```bash
flutter run
```

---

### 2️⃣ الإعداد الأولي

قبل الاختبار، تأكد من:

#### ✅ قاعدة بيانات Supabase
يجب إنشاء الجداول التالية:

**جدول `hospitals`:**
```sql
CREATE TABLE hospitals (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id),
  hospital_name TEXT NOT NULL,
  district TEXT NOT NULL,
  contact_person TEXT NOT NULL,
  is_verified BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**جدول `blood_requests`:**
```sql
CREATE TABLE blood_requests (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  requester_id UUID REFERENCES users(id),
  requester_type TEXT NOT NULL CHECK (requester_type IN ('hospital', 'patient')),
  blood_type TEXT NOT NULL,
  district TEXT NOT NULL,
  urgency_level TEXT DEFAULT 'normal' CHECK (urgency_level IN ('normal', 'urgent')),
  patient_name TEXT NOT NULL,
  description TEXT,
  status TEXT DEFAULT 'active' CHECK (status IN ('active', 'completed', 'cancelled')),
  created_at TIMESTAMP DEFAULT NOW()
);
```

**جدول `districts` (إذا لم يكن موجوداً):**
```sql
CREATE TABLE districts (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name_ar TEXT NOT NULL,
  is_active BOOLEAN DEFAULT TRUE
);

-- إضافة مديريات المهرة
INSERT INTO districts (name_ar) VALUES
  ('الغيضة'),
  ('حات'),
  ('حصوين'),
  ('الجديد'),
  ('قشن'),
  ('شحن'),
  ('المسيلة'),
  ('حوف'),
  ('سيحوت');
```

#### ✅ تكوين Supabase
تأكد من تحديث الملف `lib/config/supabase_config.dart`:
```dart
static const String supabaseUrl = 'YOUR_SUPABASE_URL';
static const String supabaseAnonKey = 'YOUR_SUPABASE_ANON_KEY';
```

---

### 3️⃣ سيناريوهات الاختبار

#### 🏥 **السيناريو 1: تسجيل مستشفى**

1. افتح التطبيق
2. سجّل دخول برقم هاتف جديد (استخدم رقم اختبار Firebase)
3. اختر نوع المستخدم: "مستشفى"
4. املأ نموذج التسجيل:
   - اسم المستشفى: "مستشفى الغيضة العام"
   - المديرية: "الغيضة"
   - الشخص المسؤول: "د. أحمد محمد"
5. اضغط "إرسال طلب التسجيل"
6. **النتيجة المتوقعة:**
   - رسالة نجاح: "تم إرسال الطلب بنجاح"
   - ملاحظة: "سيتم مراجعة طلبك من قبل الإدارة"

#### 🩸 **السيناريو 2: إنشاء طلب دم (مستشفى)**

1. بعد تسجيل المستشفى، انتقل لإنشاء طلب دم
2. املأ النموذج:
   - فصيلة الدم: "O+"
   - المديرية: "الغيضة"
   - اسم المريض: "محمد علي"
   - درجة الإلحاح: "عاجل"
   - الوصف: "حالة طارئة - حادث مروري"
3. اضغط "إرسال الطلب"
4. **النتيجة المتوقعة:**
   - رسالة: "تم إنشاء الطلب بنجاح! ✅"
   - عدد المتبرعين المتطابقين (يعتمد على البيانات)
   - خيار "عرض المتبرعين"

#### 👥 **السيناريو 3: عرض قائمة المتبرعين**

1. بعد إنشاء الطلب، اضغط "عرض المتبرعين"
2. **النتيجة المتوقعة:**
   - قائمة بالمتبرعين المتوافقين
   - لكل متبرع:
     - الاسم
     - فصيلة الدم (بلون أحمر)
     - المديرية
     - عدد التبرعات
     - العمر
     - أزرار التواصل:
       - 📞 اتصال
       - 💬 واتساب
       - ✈️ تليجرام (إذا كان متاحاً)

#### 📞 **السيناريو 4: التواصل مع متبرع**

1. من قائمة المتبرعين، اضغط على زر "اتصال"
2. **النتيجة المتوقعة:**
   - يفتح تطبيق الهاتف مع رقم المتبرع

3. اضغط على زر "واتساب"
4. **النتيجة المتوقعة:**
   - يفتح تطبيق واتساب مع محادثة المتبرع

#### 🧪 **السيناريو 5: توافق فصائل الدم**

اختبر أن النظام يعرض المتبرعين المتوافقين فقط:

**مثال 1: طلب فصيلة AB+**
- يجب أن يعرض متبرعين من جميع الفصائل (O-, O+, A-, A+, B-, B+, AB-, AB+)

**مثال 2: طلب فصيلة O-**
- يجب أن يعرض متبرعين من فصيلة O- فقط

**مثال 3: طلب فصيلة A+**
- يجب أن يعرض متبرعين من: O-, O+, A-, A+

---

### 4️⃣ نقاط الفحص

#### ✅ الواجهة
- [ ] جميع النصوص بالعربية
- [ ] الخط: IBM Plex Sans Arabic
- [ ] الاتجاه من اليمين لليسار (RTL)
- [ ] الألوان متناسقة
- [ ] الأيقونات واضحة

#### ✅ الوظائف
- [ ] تسجيل المستشفى يعمل
- [ ] إنشاء طلب الدم يعمل
- [ ] البحث عن متبرعين يعمل
- [ ] توافق فصائل الدم صحيح
- [ ] أزرار التواصل تعمل

#### ✅ التحقق من البيانات
- [ ] الحقول المطلوبة تظهر رسائل خطأ
- [ ] القوائم المنسدلة تعمل
- [ ] حالات التحميل تظهر
- [ ] رسائل النجاح/الخطأ واضحة

---

### 5️⃣ المشاكل المحتملة

#### ❌ "لا يوجد متبرعين"
**السبب:** لا توجد بيانات متبرعين في قاعدة البيانات
**الحل:** أضف متبرعين تجريبيين في جدول `donors`

#### ❌ "خطأ في الاتصال"
**السبب:** إعدادات Supabase غير صحيحة
**الحل:** تحقق من `supabase_config.dart`

#### ❌ "القوائم فارغة"
**السبب:** جدول `districts` فارغ
**الحل:** أضف المديريات كما في الخطوة 2

---

### 6️⃣ بيانات اختبار مقترحة

#### متبرعين تجريبيين:
```sql
-- متبرع O+
INSERT INTO donors (user_id, full_name, blood_type, district, is_available, has_chronic_diseases, donation_count, age)
VALUES ('USER_ID_1', 'أحمد محمد', 'O+', 'الغيضة', true, false, 2, 28);

-- متبرع A+
INSERT INTO donors (user_id, full_name, blood_type, district, is_available, has_chronic_diseases, donation_count, age)
VALUES ('USER_ID_2', 'سالم علي', 'A+', 'الغيضة', true, false, 1, 32);

-- متبرع O-
INSERT INTO donors (user_id, full_name, blood_type, district, is_available, has_chronic_diseases, donation_count, age)
VALUES ('USER_ID_3', 'خالد سعيد', 'O-', 'الغيضة', true, false, 5, 35);
```

---

## 📊 النتيجة المتوقعة

بعد الاختبار الناجح، يجب أن:
- ✅ تسجيل المستشفيات يعمل بشكل صحيح
- ✅ إنشاء طلبات الدم يعمل
- ✅ البحث عن متبرعين متوافقين يعمل
- ✅ التواصل مع المتبرعين يعمل
- ✅ الواجهة سلسة وسهلة الاستخدام

---

## 🚀 الخطوة التالية

إذا كانت جميع الاختبارات ناجحة، يمكننا الانتقال إلى:
**المرحلة 4: الشاشة الرئيسية للمستشفى**

---

## 📝 ملاحظات

- استخدم أرقام اختبار Firebase للمصادقة
- تأكد من تفعيل الإنترنت
- تحقق من صلاحيات التطبيق (الهاتف، التخزين)
- راجع logs في حالة حدوث أخطاء

**للحصول على logs:**
```bash
flutter logs
```

---

**تم إعداد هذا الدليل بواسطة Antigravity AI** 🤖
