
# 📋 دليل جمع الأخطاء من الكونسول

## 🎯 الهدف
جمع رسائل الأخطاء من كونسول التطبيق لتحليلها وإصلاحها.

---

## 📱 الطريقة 1: استخدام Android Studio / VS Code

### الخطوات:

1. **شغّل التطبيق:**
   ```bash
   flutter run
   ```

2. **افتح نافذة الكونسول** في Android Studio أو VS Code

3. **جرّب العملية التي تسبب المشكلة:**
   - مثلاً: تسجيل متبرع جديد
   - أو: إنشاء طلب دم

4. **انسخ الأخطاء:**
   - ابحث عن رسائل تبدأ بـ:
     - `❌` (علامة خطأ)
     - `ERROR`
     - `Exception`
     - `Failed`
   - انسخ **كل** السطور من بداية الخطأ حتى نهايته

---

## 🖥️ الطريقة 2: استخدام Flutter Logs

### في نافذة Terminal جديدة:

```bash
cd d:\mahrah_blood_bank
flutter logs
```

**ثم:**
1. جرّب العملية التي تسبب المشكلة
2. انسخ الأخطاء التي تظهر

---

## 📊 الطريقة 3: استخدام ADB (للأجهزة الحقيقية)

```bash
adb logcat | findstr "flutter"
```

**أو للحصول على كل الأخطاء:**
```bash
adb logcat | findstr "ERROR"
```

---

## 🔍 ما الذي يجب نسخه؟

### ✅ انسخ هذه الأنواع من الرسائل:

#### 1. أخطاء Supabase:
```
❌ خطأ في إنشاء المتبرع: ...
❌ خطأ في إدراج المستشفى: ...
❌ خطأ في إنشاء الطلب: ...
```

#### 2. أخطاء PostgreSQL:
```
PostgrestException
code: ...
message: ...
details: ...
```

#### 3. أخطاء الشبكة:
```
SocketException
HttpException
Connection refused
```

#### 4. أخطاء Flutter:
```
Exception: ...
Error: ...
Unhandled Exception: ...
```

---

## 📝 مثال على خطأ كامل يجب نسخه:

```
❌ خطأ في إنشاء المتبرع: PostgrestException(message: null value in column "user_id" violates not-null constraint, code: 23502, details: Failing row contains (abc-123, null, أحمد محمد, O+, الغيضة, ...), hint: null)
```

---

## 🎯 السيناريوهات المختلفة

### 1️⃣ اختبار تسجيل متبرع:

```bash
# شغّل التطبيق
flutter run

# في التطبيق:
1. اختر "متبرع"
2. أدخل رقم الهاتف
3. أدخل OTP
4. املأ البيانات
5. اضغط "تسجيل"

# إذا ظهر خطأ، انسخ كل ما يظهر في الكونسول
```

### 2️⃣ اختبار تسجيل مستشفى:

```bash
# في التطبيق:
1. اختر "مستشفى / مركز صحي"
2. أدخل رقم الهاتف
3. أدخل OTP
4. املأ البيانات
5. اضغط "إرسال طلب التسجيل"

# انسخ أي أخطاء تظهر
```

### 3️⃣ اختبار إنشاء طلب دم:

```bash
# في التطبيق:
1. اختر "محتاج لدم"
2. أدخل رقم الهاتف
3. أدخل OTP
4. املأ تفاصيل الطلب
5. اضغط "إرسال الطلب"

# انسخ أي أخطاء تظهر
```

---

## 📤 كيف ترسل الأخطاء؟

### الطريقة المثلى:

1. **افتح ملف نصي جديد**
2. **الصق الأخطاء فيه**
3. **أضف معلومات السياق:**
   ```
   العملية: تسجيل متبرع جديد
   الخطوة: بعد الضغط على زر "تسجيل"
   
   الخطأ:
   [الصق الخطأ هنا]
   ```
4. **أرسل النص كاملاً**

---

## 🔧 أمثلة على أخطاء شائعة

### خطأ 1: مشكلة في RLS
```
PostgrestException(message: new row violates row-level security policy, code: 42501)
```
**الحل:** نفّذ `fix_rls_policies.sql`

### خطأ 2: حقل مفقود
```
PostgrestException(message: column "age" does not exist, code: 42703)
```
**الحل:** نفّذ `ALTER TABLE donors ADD COLUMN age INTEGER;`

### خطأ 3: قيمة NULL
```
PostgrestException(message: null value in column "user_id" violates not-null constraint, code: 23502)
```
**الحل:** تحقق من أن `user_id` يتم إرساله بشكل صحيح

### خطأ 4: مشكلة اتصال
```
SocketException: Failed host lookup: 'jjooxmznzjuduhbqlihi.supabase.co'
```
**الحل:** تحقق من اتصال الإنترنت

---

## ✅ قائمة التحقق

قبل إرسال الأخطاء، تأكد من:

- [ ] نسخت **كل** رسالة الخطأ (من البداية للنهاية)
- [ ] ذكرت **أي عملية** كنت تقوم بها
- [ ] ذكرت **متى** ظهر الخطأ (بعد الضغط على أي زر)
- [ ] نسخت أي رسائل إضافية قبل أو بعد الخطأ

---

## 🚀 بعد إرسال الأخطاء

سأقوم بـ:
1. ✅ تحليل الخطأ
2. ✅ تحديد السبب
3. ✅ إنشاء الحل
4. ✅ اختبار الحل

---

**الآن جرّب التطبيق وانسخ أي أخطاء تظهر في الكونسول! 📋**
