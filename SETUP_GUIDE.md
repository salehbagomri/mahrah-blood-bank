# 🚀 خطوات تشغيل التطبيق

## ✅ ما تم إنجازه

1. ✅ إنشاء جميع ملفات المشروع (Android, iOS, Web, etc.)
2. ✅ تحديث اسم الحزمة إلى: `com.bagomri.mahrahbloodbank`
3. ✅ إعداد Firebase للأندرويد
4. ✅ إضافة أذونات الإنترنت
5. ✅ تحديث اسم التطبيق إلى "بنك الدم - المهرة"
6. ✅ إيقاف Gradle Daemon

---

## 📱 خطوات التشغيل الآن

### الطريقة 1: التشغيل المباشر (موصى بها)

```powershell
# في مجلد المشروع
cd d:\mahrah_blood_bank

# تشغيل التطبيق
flutter run -d 22101320G
```

إذا واجهت مشكلة في Kotlin Daemon، جرب:

```powershell
# إيقاف جميع عمليات Gradle
cd android
.\gradlew --stop
cd ..

# ثم حاول مرة أخرى
flutter run -d 22101320G
```

### الطريقة 2: البناء ثم التثبيت

```powershell
# بناء APK
flutter build apk --debug

# ثم تثبيت يدوياً على الجهاز
# الملف سيكون في: build\app\outputs\flutter-apk\app-debug.apk
```

---

## 🔧 إعداد Firebase (مهم!)

### الخطوات:

1. **اذهب إلى Firebase Console**:
   - https://console.firebase.google.com

2. **اختر مشروعك** أو أنشئ مشروع جديد

3. **أضف تطبيق Android**:
   - اسم الحزمة: `com.bagomri.mahrahbloodbank`
   - اسم التطبيق: بنك الدم - المهرة

4. **حمّل ملف google-services.json**:
   - بعد إضافة التطبيق، ستحصل على ملف `google-services.json`
   - ضعه في: `d:\mahrah_blood_bank\android\app\google-services.json`
   - **استبدل** الملف الموجود حالياً (placeholder)

5. **فعّل Phone Authentication**:
   - في Firebase Console > Authentication > Sign-in method
   - فعّل "Phone"
   - أضف مزود SMS (مثل Twilio أو Firebase's test phone numbers)

---

## 📝 إعداد Supabase (تم بالفعل ✅)

- ✅ تم إضافة URL و Anon Key
- ⚠️ تأكد من تشغيل SQL Schema في Supabase:
  - افتح ملف `database_schema.sql`
  - انسخ المحتوى
  - في Supabase Dashboard > SQL Editor
  - الصق وشغّل الكود

---

## ⚠️ ملاحظات مهمة

### 1. مشكلة Kotlin Daemon
إذا ظهرت رسالة "Could not connect to Kotlin compile daemon":

```powershell
# الحل 1: إيقاف Gradle
cd android
.\gradlew --stop
cd ..

# الحل 2: تنظيف المشروع
flutter clean
flutter pub get

# الحل 3: إعادة تشغيل الكمبيوتر (في حالات نادرة)
```

### 2. الخطوط العربية
- تم إزالة تعريف خط Cairo من pubspec.yaml
- سيستخدم التطبيق الخط الافتراضي للنظام
- لإضافة خط Cairo لاحقاً:
  1. حمّل ملفات الخط (.ttf)
  2. ضعها في `assets/fonts/`
  3. أضف التعريف في pubspec.yaml

### 3. Firebase لن يعمل بدون google-services.json الحقيقي
- الملف الحالي placeholder فقط
- يجب استبداله بالملف الحقيقي من Firebase Console

---

## 🎯 الخطوات التالية بعد التشغيل

1. ✅ تأكد من ظهور شاشة Splash
2. ✅ تصفح شاشات Onboarding
3. ✅ جرب اختيار نوع الحساب
4. 🔜 إكمال شاشة إدخال رقم الهاتف و OTP
5. 🔜 إكمال باقي الوظائف

---

## 📞 في حالة المشاكل

### مشكلة: التطبيق لا يبني

```powershell
flutter clean
flutter pub get
cd android
.\gradlew --stop
cd ..
flutter run -d 22101320G
```

### مشكلة: خطأ في Firebase

- تأكد من وجود `google-services.json` الحقيقي
- تأكد من تطابق اسم الحزمة في Firebase مع `com.bagomri.mahrahbloodbank`

### مشكلة: خطأ في Supabase

- تأكد من تشغيل SQL Schema
- تأكد من صحة URL و Anon Key في `lib/config/supabase_config.dart`

---

## ✨ معلومات المشروع

- **اسم الحزمة**: com.bagomri.mahrahbloodbank
- **اسم التطبيق**: بنك الدم - المهرة
- **minSdk**: 21 (Android 5.0+)
- **اللغة**: العربية (RTL)

---

**بالتوفيق! 🩸**
