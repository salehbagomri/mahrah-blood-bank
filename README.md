# 🩸 بنك الدم - محافظة المهرة

تطبيق Flutter لربط المتبرعين بالدم مع المحتاجين في محافظة المهرة، اليمن.

## 📱 نظرة عامة

تطبيق بنك الدم هو منصة مجانية تهدف إلى تسهيل عملية إيجاد متبرعين بالدم في محافظة المهرة. التطبيق يربط بين:
- **المتبرعين**: الأشخاص الراغبين في التبرع بالدم
- **المستشفيات**: المراكز الصحية التي تحتاج لمتبرعين
- **المحتاجين**: الأفراد الذين يبحثون عن متبرع بفصيلة دم معينة

## ✨ المميزات

- 🔐 تسجيل دخول آمن عبر رقم الهاتف (OTP)
- 🩸 قاعدة بيانات شاملة للمتبرعين بجميع فصائل الدم
- 🔍 البحث عن متبرعين حسب الفصيلة والمديرية
- 🏥 دعم المستشفيات والمراكز الصحية
- 📱 إشعارات فورية عند وجود طلبات دم جديدة
- 🌙 واجهة عربية كاملة مع دعم RTL
- 🎨 تصميم عصري وسهل الاستخدام

## 🛠️ التقنيات المستخدمة

- **Flutter**: إطار العمل الأساسي
- **Supabase**: قاعدة البيانات والمصادقة
- **Firebase**: الإشعارات (FCM)
- **Provider**: إدارة الحالة
- **Cairo Font**: الخط العربي

## 📋 المتطلبات

- Flutter SDK (3.0.0 أو أحدث)
- Dart SDK
- Android Studio / VS Code
- حساب Supabase
- حساب Firebase (اختياري للإشعارات)

## 🚀 التثبيت والإعداد

### 1. استنساخ المشروع

```bash
cd d:\mahrah_blood_bank
```

### 2. تثبيت المكتبات

```bash
flutter pub get
```

### 3. إعداد Supabase

1. أنشئ مشروع جديد على [Supabase](https://supabase.com)
2. انسخ `URL` و `Anon Key` من لوحة التحكم
3. افتح ملف `lib/config/supabase_config.dart` وأضف البيانات:

```dart
static const String supabaseUrl = 'YOUR_SUPABASE_URL';
static const String supabaseAnonKey = 'YOUR_SUPABASE_ANON_KEY';
```

4. شغّل السكريبت SQL من ملف `database_schema.sql` في SQL Editor بـ Supabase

### 4. إعداد Firebase (اختياري)

#### Android:
1. أنشئ مشروع Firebase
2. أضف تطبيق Android
3. حمّل ملف `google-services.json`
4. ضعه في `android/app/`

#### iOS:
1. أضف تطبيق iOS في Firebase
2. حمّل ملف `GoogleService-Info.plist`
3. ضعه في `ios/Runner/`

### 5. إعداد Phone Authentication في Supabase

1. اذهب إلى ** ** > **Providers**
2. فعّل **Phone**
3. اختر مزود SMS (مثل Twilio)
4. أضف بيانات الاعتماد

## ▶️ تشغيل التطبيق

```bash
flutter run
```

أو للتشغيل على جهاز محدد:

```bash
# Android
flutter run -d android

# iOS
flutter run -d ios
```

## 📁 بنية المشروع

```
lib/
├── main.dart                 # نقطة البداية
├── config/                   # ملفات الإعداد
│   ├── theme.dart           # الألوان والثيم
│   ├── supabase_config.dart # إعدادات Supabase
│   └── firebase_config.dart # إعدادات Firebase
├── models/                   # نماذج البيانات
│   ├── user_model.dart
│   ├── donor_model.dart
│   ├── hospital_model.dart
│   └── request_model.dart
├── providers/                # مزودات الحالة
│   └── auth_provider.dart
├── services/                 # الخدمات
│   ├── supabase_service.dart
│   └── notification_service.dart
├── screens/                  # الشاشات
│   ├── splash_screen.dart
│   ├── onboarding_screen.dart
│   ├── login_screen.dart
│   └── home_screen.dart
├── widgets/                  # المكونات القابلة لإعادة الاستخدام
│   └── custom_button.dart
└── utils/                    # الأدوات المساعدة
    ├── constants.dart
    └── blood_types.dart
```

## 🩸 فصائل الدم المدعومة

التطبيق يدعم جميع فصائل الدم:
- A+ / A-
- B+ / B-
- AB+ / AB-
- O+ / O-

مع نظام ذكي للتوافق بين الفصائل.

## 🗺️ المديريات المدعومة

- الغيضة
- سيحوت
- حصوين
- قشن
- حوف
- شحن
- المسيلة
- حات
- الحدبين

## 🔮 الخطوات التالية

- [ ] إكمال شاشة إدخال رقم الهاتف و OTP
- [ ] إنشاء شاشة تسجيل بيانات المتبرع
- [ ] إنشاء شاشة البحث عن متبرعين
- [ ] إنشاء شاشة طلبات الدم
- [ ] إضافة نظام الإشعارات الكامل
- [ ] إنشاء لوحة تحكم للمستشفيات
- [ ] إضافة خريطة لعرض المتبرعين
- [ ] إضافة نظام التقييمات والمراجعات

## 🤝 المساهمة

المشروع مفتوح المصدر ونرحب بأي مساهمات لتحسينه.

## 📄 الترخيص

هذا المشروع مجاني ومفتوح المصدر لخدمة أهالي محافظة المهرة.

## 📞 التواصل

للاستفسارات أو الدعم الفني، يرجى التواصل عبر:
- البريد الإلكتروني: [أضف بريدك]
- الهاتف: [أضف رقمك]

---

**ملاحظة**: هذا التطبيق في مرحلة التطوير الأولية. بعض الميزات قد لا تكون متاحة بعد.

**صُنع بـ ❤️ لأهالي المهرة**
